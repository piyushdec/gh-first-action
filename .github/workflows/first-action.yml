name: First Workflow
on: workflow_dispatch
jobs:
  first-job:
    runs-on: macos-14-arm64
    steps:
      - name: Print Greeting
        run: echo "Hello World"
      - name: Print Goodbye
        run: echo "Done - bye!"
    
    
